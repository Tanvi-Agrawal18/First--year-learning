#include <stdio.h>
int main()
{
  float celsius,farenheit;
  printf("Enter temperature in celsius");
  scanf("%f", &celsius);
farebheit= (celsius × 9/5)+32
  printf("temperature in Fahrenheit in %.2f," farenheit);

return 0;
}
  
